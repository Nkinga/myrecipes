<div class="row">
  <div class="col-md-2 col-md-offset-2">
    <% flash.each do |name, msg| %>
      <div class="alert alert-<%= name %> btn-xl">
        <a href="#" class="close" data-dismiss="alert">&#215;</a>
        <%= msg %>
      </div>
    <% end %>
  </div>
</div>